services:
  certipulse:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: certipulse_app

    restart: unless-stopped

    ports:
      - "127.0.0.1:3000:3000"

    env_file:
      - .env

    volumes:
      - ./prisma:/app/prisma